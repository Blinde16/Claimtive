# Postgres connection string used by Prisma
DATABASE_URL="postgresql://postgres:claimtive@localhost:5432/claimtive?schema=public"

# Secret used to sign session JWTs. Generate with: openssl rand -base64 32
AUTH_SECRET="change-me-to-a-long-random-string"

# --- AI layer (Claude via Vertex AI) ---
# All three must be set for the AI insights path to activate; otherwise the
# deterministic rule-based insights run. Claude is called from Cloud Run
# (App Hosting) directly to Vertex within the same GCP project, under the
# Google Cloud BAA — no external vendor.
AI_ENABLED="false"
VERTEX_PROJECT_ID="claimtive"
VERTEX_REGION="us-east5"
# The model identifier as listed in Vertex Model Garden after acceptance,
# e.g. "claude-sonnet-4-6@<snapshot-date>"
VERTEX_CLAUDE_MODEL=""
